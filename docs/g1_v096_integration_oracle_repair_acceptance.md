# G1 v0.96 integration-oracle repair acceptance

## Exact base and scope

- Exact base commit: `e7265009c15931c91a406e4a3875c464b158ca20`.
- Exact base tree: `407c4ebd387cc566ffe24375410f689c48786bbf`.
- Product version remains `0.95`; project schema remains `27`.
- This repair changes tests, one historical review fixture and this acceptance
  record only. It changes no runtime, solver, UI, report, manual, persistence,
  schema, package or version surface.

## G1 failures and accepted correction

The first complete pre-bump regression exposed three stale integration-oracle
assumptions:

1. The v0.95 programme test still asserted live project schema 26 and retained
   byte vectors generated by that schema. PR06 intentionally advanced the
   current schema to 27 for `gamma_V`. The frozen-clock recipe is rerun against
   exact current `project_io`; only the resulting byte sizes and SHA-256 values
   are refreshed.
2. The complete reproducible reference intentionally contains failed or
   invalid governing checks. PR02 correctly renders their aggregate Results
   Overview summary as a Streamlit error. The fixture now requires that exact
   calculation-status message while continuing to reject application
   exceptions, warnings and any additional error.
3. PR08 added visible report contents. The two crack-width worked headings are
   therefore present once in navigation and once in the body each. The
   extracted-text inventory now expects four occurrences without changing the
   two-calculation body depth.

## Adversarial acceptance matrix

| ID | Case | Required outcome |
|---|---|---|
| G1-IO-01 | Live identity is inspected | Product is 0.95 and current project schema is exactly 27. |
| G1-IO-02 | Frozen-clock upload vectors are regenerated | Both valid payloads parse; exact current sizes and hashes match; the invalid first byte still fails UTF-8 decoding. |
| G1-IO-03 | Reproducible reference is calculated | No application exception or warning; exactly the intended failed/invalid governing-summary message is emitted. |
| G1-IO-04 | Audit reference report is extracted | Both crack-width worked headings occur in contents and body; every main calculation chapter remains present. |
| G1-IO-05 | Reference numerical checks continue | Plastic, elastic, cracking, fatigue, member and detailing values retain their independent-oracle matches. |
| G1-IO-06 | Repository scope is inspected | No application or governed version/schema file changes. |

## Required evidence

1. The isolated v0.95 programme-document and reproducible-reference suites.
2. Complete G1 static, numerical, UI, publication, real-image, package and
   portable-startup qualification after this repair merges.
3. Compile, targeted Ruff, diff, version, schema and changed-path guards.
