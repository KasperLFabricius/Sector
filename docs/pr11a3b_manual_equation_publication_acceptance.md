# PR-11A3b acceptance - visible manual equation publication

## Exact base and bounded purpose

- Exact accepted base: 141b1a9cb0ebed18d7ed84124d54327bae48909c.
- Base tree: 02d449ddf203515dc2bc1b2e6bd77ddeacc856e8.
- Base parent: 3e05c71ebb65ddc3ea8a00f8d7f2f81fcfce2c5b.
- Sector remains version 0.91.
- Family: the visible Streamlit and PDF rendering half of the accepted Part C
  manual equation contract.

No rejected or superseded head is reused. The exact merged manual block stream
and the accepted location, source and semantic catalogues are the only equation
publication authority.

## Frozen visible inventory

Both manual surfaces publish the same exact ordered set of 32 equations. Each
visible block contains:

- stable public number `C3-1` through `C11-2` and deterministic anchor;
- the exact accepted expression, without a renderer-selected formula;
- dimensional class;
- every ordered result identity, meaning and unit;
- exact source classification and source text;
- every ordered symbol, meaning and unit;
- every genuine ordered dependency as a stable equation-number reference.

The complete retained inventory remains 32 equations, 207 symbol records, 46
result records and 21 dependency leaves. Project-defined methods remain
explicitly uncited. Standard and mixed records retain the exact accepted local
Design Basis provenance; the renderer adds no citation.

## Surface mechanics

- The raw authored manual remains unchanged and continues to be the single
  content source for app and PDF.
- A fail-closed pure publication layer binds the live block stream through the
  accepted location, source and semantic contracts before segmenting displays.
- All Part C non-display text is preserved exactly and in order.
- Streamlit shows number, formula, result, dependencies and source immediately.
  Its 207-row symbol inventory is available through 32 collapsed, equation-local
  expanders so the ordinary manual view stays light and readable.
- The PDF prints the same information and complete symbol tables for offline
  hand calculations. Dependency references use the stable equation anchors.
- The existing Markdown/KaTeX and ReportLab conversion paths remain the only
  surface-specific mathematical renderers.

## Failure and hostile closure

- Missing, extra, altered, reordered, moved or unpaired Part C displays fail
  before either surface can publish them.
- Renderers receive one exact `ContractedManualEquation`; they cannot select or
  override identity, expression, source, symbols, results or dependencies.
- Unknown types, changed semantic catalogues and coherently resealed downstream
  contract replacements fail closed.
- Every advertised source, result, symbol, unit, dimension and dependency is
  checked for visible reachability; both surfaces retain all 32 identities.

## Explicit exclusions

- No formula, solver, material law, resistance, demand, utilization, verdict or
  standard applicability change.
- No generated-report equation or layout change.
- No Figure/Table numbering, captions, repeated units or grayscale work
  (PR-11B).
- No shared publication style or structural/raster PDF preflight (PR-11C).
- No calculation-trace restoration, schema, persistence, workflow, package,
  signing, version, PR-12+, release or v0.93 work.
