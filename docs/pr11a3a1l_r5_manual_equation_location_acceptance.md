# PR-11A3a1L R5 - manual equation identity and authored location

## Exact base and independent recut

- Base commit: `e25e730c5129a1b5f5a9a194e6bb91e2e5f761cf`.
- Base tree: `e6e3de5d543893b8f39ee63ec2f4f335fd68c8dc`.
- Base parent: `6d3336867ddaa449f9887551c96b76125dedb6c5`.
- Application version: `0.91` and unchanged.
- This candidate is authored independently from accepted `main` and the live
  Part C manual blocks. No file, patch, commit or ancestry is reused from a
  rejected PR head.
- All 42 rejected/superseded heads known at branch cut are absent from ancestry,
  including both closed PR #306 heads `f86aec2` and `ed9e997`.

## Frozen boundary

- Exactly 32 display equations inside `Part C - Theory & methodology` are in
  scope. Display equations added to Parts A, B or D are explicitly inert.
- Stable authored order, ordinal, semantic key and public number run from `C3-1`
  through `C11-2`.
- Every record retains exact part, section, subsection and normalized-expression
  SHA-256.
- Missing, duplicate, reordered, moved, unknown, altered and coherently resealed
  location catalogues fail closed.
- Every textual field of an excluded Part C block shape is recursively inspected;
  a display cannot hide in a callout, nested table cell or unknown sibling field.
- Unpaired Part C display delimiters and non-string block identities fail closed.
- The identical Formula (6.29) expressions retain distinct torsion `C10-2` and
  combined `C11-1` identities.
- H1-level equations retain an empty subsection as a deliberate location value;
  it is not filled from an earlier unrelated H2.

## Explicit exclusions

- No standard, project or mixed source kind and no citation text. Provenance is
  an independent successor slice and cannot alter this candidate.
- No symbol, unit, dimensional or dependency inventory.
- No PDF or Streamlit rendering and no visible publication block.
- No solver, formula, mechanics, resistance, demand, utilization, verdict,
  trace, report-equation or application behavior change.
- No figure/table numbering, caption, repeated-unit, grayscale, shared-style or
  PDF-preflight work.
- No schema, persistence, workflow, package, signing, version, PR-12+, release or
  v0.93 work.
