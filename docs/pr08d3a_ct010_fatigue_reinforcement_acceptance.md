# PR-08D.3a CT-010 acceptance (reinforcement fatigue)

This matrix freezes a PR-08D fatigue slice from retained main mechanics.
Sector remains `0.91`. One registry `sector-ct-010-fatigue-v1`, family
`ct-010-fatigue`, starting with the reinforcement members: the per-element
reinforcement fatigue assessment (S-N damage and yield screening over the
grouped spectra) and the retained invalid-input branch. The concrete fatigue
members (fcd,fat, Miner/equivalent, the adaptive fibre search) are the named
next slice (PR-08D.3b) and extend this registry; every present member is
independently mandatory.

| Family or invariant | Frozen acceptance |
| --- | --- |
| Applicability | The family exists exactly when the retained fatigue adapter publishes: `fatigue_on` true. The retained invalid branch (`fatigue_analysis.validation_errors` non-empty -> `invalid_result` with its exact key order including the `valid` key that the valid payload does NOT carry - the asymmetry is pinned, never normalised) is a legitimate failed evidence state selected from validated direct inputs before candidate numerics. Fatigue is computed once per run (case-table independent) and never suppresses other results; no masking in either direction. Reinforcement members exist per the retained element/detail assignments when `fatigue_check_steel` is true; a false flag publishes no reinforcement members and supplied candidates fail. |
| Upstream boundary | The per-bin elastic solves run inside the retained fatigue engine (`solve_fatigue_bin` wrapping `solve_elastic_combined`); the trace replays the authoritative engine call per spectrum and binds the engine's published bin states as evidence, exactly as retained. Complete immutable section-geometry and assigned-material vectors, including concrete material identity, reach every reinforcement member final; the spectrum tables (name, cycles, six actions per bin, description) are input-derived with exact retained normalisation, order and cardinality. |
| Replay | Exact replay through the authoritative retained kernels: `steel_fatigue_life` (EN 1992-1-1:2023 E.5.2 / 2004+A1 6.8.4 Tables 6.3N-6.4N S-N curve with n*, k1, k2, delta-sigma-Rsk, gamma_s, gamma_ff), the mixed-bond correction (`_mixed_bond_parameters` / bond-ratio xi and equivalent diameter), Palmgren-Miner accumulation over bins (`assess_reinforcement_spectrum`), the yield screening with its retained limits, the design-action factor path, and the retained governing selections (per-element governing damage/yield bins, per-spectrum governing element, family governing spectrum). Retained edition switch (2005 / 2005+DK NA / 2023) is an implemented method identity with per-edition sources; unknown editions fail closed. Retained non-finite outputs (infinite damage, non-converged bins) seal as the established explicit result states with retained convergence flags replayed exactly - never fabricated finite numerics and never fail-closed on retained-published values. |
| Inventories | Frozen output shapes in exact key order: the `run_analysis` payload subset owned by this slice (`edition`, `checks.reinforcement`, `basis`, `method_reference`, `calculation_references`, `warnings`, `partial_factors`, `reinforcement_properties`, `fatigue_detail_basis`, `t0_days`, `elements`, `spectra` reinforcement parts, `governing_spectrum`, `utilisation`, `converged`, `passed` as they concern reinforcement), the `ReinforcementFatigueProperties`/`ReinforcementBinResult`/`ReinforcementFatigueResult` dataclass field orders, and the invalid payload's exact order. Concrete-side keys are named excluded siblings until PR-08D.3b: their presence, position and retained type stay pinned while their values remain outside this slice. Unknown, missing, duplicate or reordered retained keys fail closed at every layer. |
| Identity and typing | The five standing principles from the first line. Detail-catalog identities (detail ids, element ids, steel and concrete material ids, spectrum and bin names, and published bin descriptions) are tokenized into sealed step ids; same-curve or same-law different-ID assignments seal differently; used 2023 MATERIAL assignments never finite while the 2023 fatigue edition remains a legitimate retained method. Exact types across the whole reader surface (exact Booleans for the check flags and convergence; finite numbers for factors, cycles and stresses; exact strings for ids/labels), with the reader-surface classification frozen in this slice's report. |
| Verdicts | Reinforcement damage and yield utilisations are genuine retained demand-versus-resistance decisions: `passed`/`converged` and the damage limit 1.0 replay exactly with the literal status encoding; finals genuinely derive from the declared bin/element/spectrum dependencies (worst-first). No global verdict beyond the retained family fields; no fabricated numerics on the invalid branch. |
| Dependency closure | Every leaf (spectrum bins, detail parameters, partial factors, element identities, engine bin states) reaches its member final through exact operand-level dependencies; stale seal, resealed value/source/unit/axes/order and dependency-edge removal fail; the graph/tamper battery covers a multi-spectrum, multi-element run including a non-converged bin. |

Hard exclusions: the concrete fatigue members including fcd,fat, the
Miner/equivalent concrete methods and the adaptive fibre search (PR-08D.3b),
crack width (CT-009/PR-08D.1), SLS limits, bridges (CT-011), the chord join
(PR-08D.5), CT-002 through CT-011 mechanics, solver/formula changes,
UI/report/manual/publication/persistence/package/schema/workflow/version
changes, F-020, rejected-head work.
