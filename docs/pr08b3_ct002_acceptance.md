# PR-08B.3 CT-002 selected plastic-capacity acceptance

This matrix is frozen before implementation. The slice owns only the unpublished
solver trace, its exact registry, and adjacent hidden solver evidence. Sector
remains `0.91`; CT-003+, presentation, persistence, packaging, workflow, and
publication are excluded.

| Branch or invariant | Frozen acceptance |
| --- | --- |
| Sweep authority | Derive exact member count and angle order from requested `v_min`, `v_max`, and `v_inc`, including closed-turn endpoint removal; retained deletion or reordering fails closed. |
| Finite-selected | Every expected member is bracketed, residual-converged, and aligned with retained point/Mx/My data; the authoritative radial selector supplies one exact non-Boolean in-range member. |
| Complete finite state | Bind requested/achieved axial action, bracket/reachability/residual/tolerance, compression depth, neutral-axis and strain plane, strains, separately accumulated concrete/bar/tendon force-Mx-My, compression/tension resultants, lever, radial demand/resistance/utilisation, and verdict. |
| Failed-selected | Publish only requested sweep inputs, expected member-angle identities, Boolean convergence flags, failure count/state, and the replay selector's selected identity. |
| Failed-unselected | Publish the same minimal failure evidence without a selected identity. Failed numerical arrays and point mechanics are neither parsed nor required. |
| Failure-array inertness | Mutation, deletion, or reordering of unreported failed Mx/My, point mechanics, depth, strain, resultants, resistance, utilisation, and verdict leaves the sealed minimal failure trace byte-identical. Promotion to finite traverses the complete finite validation path. |
| Exact registry | Independently declare family/member/context/method/edition/cardinality/branch/state plus exact step order, dependencies, quantity roles, and full source/citation identity; missing CT-002 cannot be masked by another calculation. |
| Provenance | Reuse immutable shared geometry/action/material blocks. Only exact canonical standard laws retain their citations; edited or custom laws remain uncited project methods. |
| Hostile closure | Reject missing/Boolean/non-integer/out-of-range/contradictory selection, total-preserving material swaps, radial drift, graph/metadata/citation drift, stale hashes, bad seals, masking, and coherent sweep tamper. |
| Independent oracle | A test-only search derives every angle, brackets and solves every axial root from original inputs, derives governing curvature itself from the original material limits and projections, accumulates through only the authoritative low-level mechanics kernel, and compares the full mixed concrete/bar/tendon sweep plus an independent radial selection without consuming candidate solver state. |
