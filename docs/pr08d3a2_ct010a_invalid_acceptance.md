# PR-08D.3a.2 CT-010a retained invalid-member acceptance

Base is accepted `main` at `a6f578e0d6b0bb3ec472aefaa607ae27ec6f88b4`.
Sector remains `0.91`.

| Boundary | Frozen acceptance |
| --- | --- |
| Applicability | Publish exactly one CT-010a invalid member only when fatigue is enabled, reinforcement checking is exact `True`, and the retained adapter publishes its genuine exact INVALID payload. Disabled, absent, concrete-only and successful concrete-only states publish no CT-010a member. |
| Failure first | Required dispatch controls are exact built-in Booleans. Current original-input validation must yield at least one genuine error before any candidate success field, geometry, material law, solver state or numerical result is parsed. |
| Candidate closure | Require the exact ordered INVALID inventory, exact built-in `valid is False`, and complete equality with a fresh `validation_errors` plus `invalid_result` replay, including error/warning order, edition, checks, basis, method reference, factors, elements and empty result fields. |
| Minimal evidence | Publish SHA-256 words over every safe retained invalid value plus the exact inventory/type structure of arbitrary failure-only factors, age and element values; raw failure-only values remain governed by exact fresh-adapter equality and are not traversed. Publish the three dispatch states, ordered error and warning identities/counts, and one explicit `failed` final with the retained messages. Do not publish resistance, utilisation, verdict, fabricated finite substitutes or success-only mechanics. Every published leaf reaches the failed final. |
| Provenance | Dispatch leaves are input-owned. Normalisation, errors, warnings and invalid payload are Sector project-boundary evidence. No standard citation is attached to a calculation-free invalid state. |
| Inert failure data | Missing/malformed section geometry, materials, spectra and numerical success fields are not traversed after genuine invalid classification. Different failure-only object values that replay to the same retained INVALID payload produce byte-identical traces. |
| Hostile closure | Reject Boolean lookalikes, missing controls, forged/truncated/reordered/extended invalid payloads, fabricated or reordered errors/warnings, valid-input invalid payloads, concrete-only masking, unrelated-family masking, failed-to-success promotion, stale fingerprints, coherent reseals, source/role/unit/axis/dependency/final tampering and missing/extra members. |

Excluded: successful CT-010a mechanics already accepted in PR #261, concrete
fatigue CT-010b, CT-009/CT-009b, activation/publication/UI/report/manual,
solver/formula changes, PR-07 removals, F-020, v0.93 and rejected heads.
