# PR-08D.4 CT-011 acceptance (retained bridge kernels)

This matrix freezes the first PR-08D slice from retained main mechanics.
Sector remains `0.91`. One registry `sector-ct-011-bridge-v1`, family
`ct-011-bridge`, with three independently mandatory members mirroring the
retained independent component calculations: `brittle-method-b`, `box-walls`
and `minimum-crack-reinforcement`. No SLS/crack/fatigue/chord scope; those are
later PR-08D slices.

| Family or invariant | Frozen acceptance |
| --- | --- |
| Applicability | The family exists exactly when the retained bridge adapter is active: `bool(bridge_result["calculations"] or bridge_result["errors"])`. Each member exists exactly when its retained table produces rows through `bridge_inputs.calculate` for the selected standard. The retained error branch (`TypeError`/`ValueError` from the adapter, publishing `selected_standard`, `scope`, empty `calculations` and the error strings) is a legitimate finite evidence state selected before candidate numerics. Bridge activity is independent of section/geometry validity and of every other family; no masking in either direction. Unknown `bridge_standard` values raise in the retained adapter and are that error branch, not a trace failure; the error-branch axis identity tokenizes the (possibly whitespace-empty) normalized standard, and both entry points map replay exceptions identically. |
| Replay | Exact replay through the authoritative retained kernels `bridge.calculate_brittle_method_b`, `bridge.calculate_box_walls` and `bridge.calculate_minimum_crack_reinforcement` with the exact retained table normalisation (`bridge_inputs.normalise_table`/`records`). Frozen numerics: `As,min = Mrep/(zs fyk)` (DS/EN 1992-2:2005 6.1(109)-(110)); `VEd/VRd,max + TEd,wall/TRd,max,wall <= 1.0` with the retained cot-theta range warning (6.3.2(101)-(104) incl. AC:2008); `As,min = kc k fct,eff Act / sigma_s` (7.3.2(102)-(105)) with the restrained-shrinkage floor `fct,eff >= 2.9 MPa`; PASS/FAIL at `utilisation <= 1 + 1e-9`; the DK NA brittle-method warning is retained evidence (warn, never suppress). Retained kernels can publish unbounded row values (`as_required`/`utilisation` overflowing from finite entered operands) with the FAIL status; the trace seals such row values as explicit positive-infinity result states - and undefined (NaN) retained row values as explicit undefined states - with the FAIL status replayed exactly, never fabricating finite numerics; the cross-table error precedence is the retained interleaved `bridge_inputs.calculate` order (records then kernel per member). |
| Inventories | Frozen output shapes in exact key order: brittle `method, equation, source, selected_standard, warning, rows` with its 8-key rows; box walls `method, equation, source, rows, warnings` with its rows (`BoxWall` fields plus `cot_theta, utilisation, status`); minimum crack `method, equation, source, rows` with its rows (component fields plus `component, fct_eff_used_mpa, as_required_mm2, as_provided_mm2, utilisation, status`). Unknown, missing, duplicate or reordered retained keys fail closed at every layer; row order and cardinality equal the retained entered table order. |
| Identity and typing | Every retained identity string (region/wall/component ids, method/equation/source strings, `selected_standard`, warnings) is bound into the sealed evidence (tokenized step ids or exact closure comparison). The five principles apply from the start: input-derived row sets from the entered tables (never trusted candidate cardinality); total surface closure over the family's publication surfaces in both member states; total operand binding for every row (not only governing rows); exact types across the whole reader surface (no coercions; finite numbers, exact strings, exact Booleans for `restrained_shrinkage`); every computed value (utilisation, status, `fct_eff_used`, `as_required`) genuinely derived from its declared dependencies. |
| Sources | Per value: the three EN 1992-2 citations above as standard sources with the exact retained document identity (`DS/EN 1992-2:2005 + AC:2008`, DK NA:2015 where the retained selection carries it); table normalisation and status aggregation are uncited project sources. Method/equation/source strings replay exactly as retained values. |
| Verdicts | Row PASS/FAIL are the retained genuine provided-versus-required decisions with the established literal status encoding; member finals are genuine aggregations of their rows' statuses (worst-first); the family adds no global verdict. No fabricated numerics on the error branch, on empty tables, or for retained unbounded or undefined row values (sealed as positive-infinity or undefined states; member finals stay finite status codes). |
| Dependency closure | Every leaf reaches its member final through exact operand-level dependencies; stale seal, resealed value/source/unit/axes/order and dependency-edge removal fail; the full graph/tamper battery covers all three members. |

Hard exclusions: SLS numerical output, crack width, fatigue and the
chord/off_util/biaxial join (later PR-08D slices), CT-002 through CT-008
mechanics, solver/formula changes, UI/report/manual/publication/persistence/
package/schema/workflow/version changes, F-020, rejected-head work.
