# PR-08C.1a CT-006 directional shear core acceptance

This matrix freezes the replacement scope before implementation. Sector remains
`0.91`. The family owns capacity-only directional shear evidence; CT-002 plastic
state and rejected PR #225 through #228 work are not inputs or implementation
sources.

| Family or invariant | Frozen acceptance |
| --- | --- |
| Applicability, cardinality and axes | CT-006 is deliberately the retained two-active directional family: exactly `vx` then `vy` are active and present in both the candidate `directions` mapping and `active_directions` list. Their physical axes are respectively `y` and `x`. Zero-active retained output has no shear family and one-active retained output is the existing uniaxial payload; both are explicit `not applicable to CT-006` boundaries that publish no trace and must not be reported as failures or converted into invented directions. Original `auto`/`negative`/`positive` face-selector mode is a mandatory axis distinct from the independently replayed ordered face set. No cross-direction interaction or aggregate verdict is published. |
| Original-input identity | Before activity filtering, both scalar signed demands and both `shear_components` signed and absolute demands are finite non-Boolean numbers. Scalar and component signed values are exactly equal, including sign, and each magnitude equals the exact absolute signed value. Component order, axes, face modes and active flags are exact. Entered `shear_vx_bw`/`shear_vy_bw`, including zero, are distinct leaves from derived effective widths. When links are enabled, the selected capacity/link-steel ID is an independent identity axis and must resolve uniquely to an aligned mild-material catalog law. |
| Retained methods | Finite evidence covers the retained DS/EN 1992-1-1:2004+A1/AC base method and its DK NA:2024 variant. The 2023 document remains `96_Published_Not_Implemented` and cannot supply a trace citation or a finite CT-006 result in this slice. |
| Concrete mechanics | For every mandatory face, reconstruct geometry, selected longitudinal steel, effective depth, effective width, axial stress, `k`, `rho_l`, `C_Rd,c`, `v_min`, basic/floor stress, `V_Rd,c`, concrete utilisation and an independent concrete-only PASS/FAIL. DK `v_min` alone carries the exact DK source; unchanged operands and formulae keep their base sources. |
| Link mechanics | When links are enabled, reconstruct link area and ratio, lever arm, design yield, `nu_1`, `alpha_cw`, both resistances, final resistance, utilisation and PASS/FAIL. Replay the retained 1,501-point minimax selector using only `VEd/VRd,s` and `VEd/VRd,max`. DK `nu_v` alone carries the exact DK source. The uncited selector remains a Sector project method. |
| Candidate inventories | The implementation constants freeze exact ordered key inventories at aggregate, direction, face wrapper, shear payload, concrete result, links, link result, angle-limit and governing-domain layers. Every retained field is independently compared. Chord/plastic/longitudinal-force fields and unrelated torsion/combined domains are named exclusions whose values are never parsed and cannot change the trace. Missing, extra or reordered retained keys fail closed. |
| Branching and verdicts | Applicability is chosen from validated original signed/absolute demands before candidate parsing: zero/one active returns the named no-family boundary above, while exactly two active enters CT-006. Within CT-006, original inputs and authoritative low-level mechanics choose finite versus failed state before candidate numerical parsing. Failed members publish minimal explicit failure evidence; omitted, malformed or non-finite failure-only candidate numerics are inert. Finite promotion traverses complete reconstruction. Face and direction verdicts are genuine demand/resistance outcomes. |
| Dependency and provenance closure | Immutable geometry, action, material and provenance blocks feed exact operand-level dependencies. Every emitted step reaches its direction final. Per-step quantity role, full source/citation, order, dependency unit and result state are exact registry contracts. Same-kind source swaps, DK/base swaps, stale seals and coherent resealed value/verdict/cot tamper fail. |
| Deferred retained bridge | PR-08D must join accepted CT-002 sweep start/end/increment, exact sweep cardinality/order and capacity-availability state to shear chord, `off_util`, biaxial and every chord-coupled selector contribution. Capacity-only and unavailable-plastic branches remain explicit, and bridge failure cannot mask CT-006 core. No bridge mechanics are implemented here. |

Hard exclusions are CT-002 through CT-005 mechanics, CT-007/008, all chord and
plastic publication, PR-08D families, SLS/crack/fatigue/bridge implementation,
UI/report/manual/persistence/package/schema/workflow/version changes, F-020 and
all rejected-head code.
