# PR-10 publication-quality acceptance matrix

## Exact base and identity

- Base commit: `563d107d223541703f848e44c07ddfbcc22bd2d7`
- Base tree: `f9acfc68ed2ede1e41b8b8d8150746ffe300a617`
- Product: Sector cross-section analysis
- Application version: `0.91`
- Project schema: `23`
- Sources: merged code and retained payloads, accepted tests, the local controlled
  Design Basis, and QA findings F-019, F-032, F-034, F-037 and F-040 as an index.

## Frozen scope

| Finding | Accepted change | Acceptance |
|---|---|---|
| F-019 | Give every splittable report table a repeated calculation context containing the current chapter, subsection and retained assessment state. Keep Loads and Analysis settings as independent page-allocation blocks. | Small/default/dense output remains complete; continuation fragments identify their calculation without relying on a previous page; avoid the known sparse-page grouping. |
| F-032 | Enforce a 7.2 pt minimum table font, increased leading and cell padding, atomic numeric/unit spans, and typographic scientific notation. | No ordinary table text is rendered below 7.2 pt; a numeric value is not internally split; exponents are published as powers rather than raw `e` notation. |
| F-034 | Correct the transverse torsion-steel provenance on every publication surface. | `T_Rd,s` is described as derived from EN 1992-1-1 Formula (6.27) together with transverse equilibrium Formula (6.8); longitudinal `sum A_sl` remains Formula (6.28). Mechanics and values are unchanged. |
| F-037 | Increase formula/reference leading and table vertical padding in the report and manual. | Superscripts/subscripts retain visible clearance from adjacent rules and text in focused PDF inspection. |
| F-040 | Use one shared ReportLab-safe notation layer for unit powers and scientific values. | Plain `mm2`, `m2`, `m4` publication markup becomes superscript notation; scientific values use `mantissa x 10^power`; source remains ASCII-only. |

## Identity and dependency inventory

- The report retains project, section, action-set/case, family, member, axis,
  direction, fibre/face, load-case, branch, method, edition, source, warning,
  applicability, failure and verdict identities already published by merged main.
- Continuation context is presentation metadata only. It repeats the current
  chapter/subsection/status and does not select or recalculate a result.
- The notation layer receives already reconstructed publication values. It changes
  markup and wrapping only; values, units and retained provenance remain identical.
- The transverse source is the local controlled
  `DS-EN 1992-1-1 (2004) - AC (2008).pdf`: 6.3.2(1), Formula (6.27), combined with
  the transverse equilibrium in 6.2.3(3), Formula (6.8). Formula (6.28) remains the
  longitudinal reinforcement relation.

## Adversarial and rendered evidence

- Parameterised notation tests cover finite scientific values, signs, unit powers,
  unit denominators, idempotent no-break markup and ordinary decimal values.
- A 180-row focused PDF forces table continuation and checks repeated context,
  minimum span size and retained headers on every fragment.
- Cross-surface tests pin the transverse/longitudinal citation distinction in the
  solver documentation, CT-007 source identity, report, app and manual.
- Focused report/manual rendering is inspected for sparse allocation, numeric
  wrapping, exponent notation, and glyph/rule or glyph/text clearance.

## Explicit exclusions

- No solver, formula, selector, material law, result verdict, input/result schema,
  project persistence, applicability, standard edition or application-version change.
- No PR-11 stable equation IDs, where-blocks, object numbering, shared publication
  style system or general PDF preflight.
- No PR-12 through PR-14 implementation, publication/release/signing work, PR-07
  reversal, rejected-head reuse, v0.93 implementation, or ROADMAP-093-002 work.
