# Development & test dependencies (in addition to requirements.txt).
-r requirements.txt
pytest>=8.0,<9
# Parallel test runner. The suite is ~90% process-isolatable AppTest cases, so a
# full gate runs several times faster with `python -m pytest -n auto` (or `-n 4`
# on a modest box). Opt-in -- it is not forced in pytest.ini so single-test debug
# runs stay fast (no worker spawn). The numba kernel cache is on disk, so JIT is
# paid once, not per worker.
pytest-xdist>=3.0,<4
# Used only by tools/gen_handcalc_fixtures.py to read the handcalc reference PDFs.
pypdf>=4.0,<6
